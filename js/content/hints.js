// ───────────────────────────────────────────────
//  Данные — тексты тематических подсказок
//  Эпик: J (tasks.md) · Статус: заглушка — реализация в Фазе 4
//  Зависит от: config.js
//  Заменяет: — (новая сущность)
// ───────────────────────────────────────────────
//
// Схема одной записи (черновик, уточняется в Эпике J):
//   { id, text, appliesTo: 'row'|'region'|'cells' }
//
// Подсказки генерируются из уже зафиксированного расположения ловушек/ключа/золота
// (gen/room.js), а не наоборот — иначе нарушается требование «никогда не лгут».
// Примеры формулировок из concept.txt: «В этом ряду нет ловушек», «Ключ находится
// в правой половине комнаты», «Рядом спрятано золото», «В одном из углов — артефакт».
//
// TODO (Фаза 4): описать шаблоны текстов, включаются с уровня по конфигу campaign.js.

const HINTS = [];
