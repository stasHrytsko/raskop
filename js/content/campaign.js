// ───────────────────────────────────────────────
//  Данные кампании — таблица уровней 1–100
//  Эпик: L (tasks.md) · Статус: заглушка — реализация в Фазе 5
//  Зависит от: config.js
//  Заменяет: quotaFor()/trapsFor()/tierFor() из текущего config.js (плоские на 9 уровней)
// ───────────────────────────────────────────────
//
// Схема одной записи (черновик, уточняется в Эпике L):
//   { n, tier: 'обучение'|'ядро'|'маршруты'|'разнообразие'|'сложные'|'комбинации'|'финал',
//     roomCount, roomSizes: number[], quota, hiddenGoldRange: [min,max],
//     featuresEnabled: string[]  // напр. ['keys','tempArtifacts','hints','sectors']
//     isBoss: boolean }
//
// TODO (Фаза 5): наполнить 100 записей по тирам из «Прогрессия 100 уровней» (concept.txt),
// каждый 10-й — isBoss:true.

const CAMPAIGN = [];
