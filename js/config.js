// ───────────────────────────────────────────────
//  Раскоп — игровые константы (весь баланс здесь)
// ───────────────────────────────────────────────
//
//  МОДЕЛЬ:
//  FIELD = 5×5 (25 клеток): 20 безопасных + 5 ловушек.
//  Монет на поле НЕТ. Каждая безопасно вскрытая клетка = +1 в рюкзак.
//  FIRST_TAP_BLANK = true   первый тап = 0 ловушек среди соседей (честный старт)
//  MAX_CLUE = 3             реген, если у безопасной клетки >3 ловушек рядом
//
//  Множитель по числу безопасно открытых клеток за заход:
//    1–10  открытых → ×1
//    11–15 открытых → ×2   (TH2 = 11)
//    16–20 открытых → ×3   (TH3 = 16)
//
//  Квота уровня = 30, одинаковая на все 9 уровней.
// ───────────────────────────────────────────────
const SIZE = 5;            // поле SIZE × SIZE  (FIELD = 5×5)
const TRAPS = 5;           // ловушек на поле (остальные 20 клеток — безопасные)

const LEVELS_COUNT = 9;    // уровней в одном забеге
const QUOTA = 30;          // квота уровня (одинаковая на все уровни)
const DIVES = 3;           // заходов на уровень

const FIRST_TAP_BLANK = true;  // первый тап захода — 0 ловушек среди соседей
const MAX_CLUE = 3;        // цифры на поле только 1..3 (иначе реген поля)

// Пороги множителя жадности (число безопасных вскрытий за заход)
const TH2 = 11;            // ×2 с 11-й открытой клетки
const TH3 = 16;            // ×3 с 16-й открытой клетки

const REC_KEY = 'raskop_best';   // ключ рекорда в localStorage

// Квота, число ловушек и тир сложности уровня n (0-индекс).
// Квота и ловушки фиксированы — забег это «дойти как можно дальше».
const quotaFor = n => QUOTA;
const trapsFor = n => TRAPS;
const tierFor  = n => n < 3 ? 'easy' : n < 6 ? 'med' : 'hard';

// Маленький DOM-хелпер, общий для всех модулей
const $ = id => document.getElementById(id);
