// ───────────────────────────────────────────────
//  Постоянное золото — метавалюта между уровнями кампании
//  Эпик: G (tasks.md) · Статус: заглушка — реализация в Фазе 3
//  Зависит от: config.js
//  Заменяет: best/loadBest/saveBest/bumpBest из текущего game.js (расширяется)
// ───────────────────────────────────────────────
//
// TODO (Фаза 3):
// - wallet в localStorage (ключ raskop_wallet, по аналогии с текущим raskop_best),
//   не сбрасывается между уровнями/сессиями;
// - при успешном выходе: wallet += levelGoldRemaining * (firstClear ? 1.0 : 0.2);
// - levelsCleared: Set<number> — «пройден ли уровень хотя бы раз», отдельно от
//   текущего best (максимальный достигнутый), нужно для honest-репрохождения (20%);
// - spendWallet(amount) — трата на постоянные артефакты (core/artifacts-permanent.js).
