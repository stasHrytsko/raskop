// ───────────────────────────────────────────────
//  Попытка уровня — победа/поражение, единица провала = уровень целиком
//  Эпик: O (tasks.md) · Статус: заглушка — реализация в Фазе 2
//  Зависит от: config.js, core/tomb.js, core/economy.js, core/wallet.js,
//              core/artifacts-temp.js
//  Заменяет: startLevel/nextDive/tap/onTrap/endDive/clearLevel/runOver из game.js
// ───────────────────────────────────────────────
//
// TODO (Фаза 2), решение зафиксировано в tasks.md разделе 0:
// - победа: quotaProgress >= quota → выход открыт → игрок дошёл до выхода → покинул
//   гробницу (отделить «квота набрана» от «фактически вышел», сейчас это одно событие);
//   wallet += remainingGold * multiplier (core/wallet.js);
// - поражение: ловушка в ЛЮБОЙ комнате уровня (core/tomb.js) → золото уровня обнуляется,
//   temp-артефакт исчезает (core/artifacts-temp.js), весь Tomb текущей попытки
//   перегенерируется, игрок начинает уровень заново с первой комнаты;
// - прогресс кампании (levelsCleared, core/wallet.js) провал НЕ трогает — обнуляется
//   только текущая попытка;
// - постоянные артефакты и wallet переживают смерть всегда — не трогать в этом коде-пути.
