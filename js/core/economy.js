// ───────────────────────────────────────────────
//  Золото уровня — квота, расход, состояние выхода
//  Эпик: B (tasks.md) · Статус: заглушка — реализация в Фазе 1
//  Зависит от: config.js, core/tomb.js
//  Заменяет: S.pack/S.prog из текущего game.js
// ───────────────────────────────────────────────
//
// TODO (Фаза 1):
// - S.gold — золото уровня, отдельно от quotaProgress;
//   quotaProgress = gold - spentOnThisLevel (трата уменьшает именно прогресс квоты);
// - exitOpen = quotaProgress >= quota, пересчитывается при каждой трате — выход может
//   закрыться повторно после того как был открыт;
// - spendGold(amount, reason) — единая точка расхода, вызывается из активации
//   артефактов (core/artifacts-permanent.js, core/artifacts-temp.js) и покупок в комнатах.
