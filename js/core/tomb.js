// ───────────────────────────────────────────────
//  Модель гробницы — Tomb/Room, переходы между комнатами, необратимость
//  Эпик: A (tasks.md) · Статус: заглушка — реализация в Фазе 1
//  Зависит от: config.js, gen/room.js, gen/sectors.js, content/campaign.js
//  Заменяет: S.field из текущего game.js (плоское поле → гробница из 2–5 комнат)
// ───────────────────────────────────────────────
//
// TODO (Фаза 1):
// - Tomb = { rooms: Room[], currentRoomId, visited: Set }
//   Room = { size, field, collapsed:boolean, doors: Door[] }
// - переход между комнатами через дверь; при выходе из комнаты — collapsed=true,
//   комната больше не доступна;
// - единица провала — весь Tomb (см. tasks.md раздел 0): ловушка в любой комнате
//   уничтожает весь Tomb текущей попытки и запускает генерацию нового для того же
//   номера уровня; core/wallet.js и прогресс кампании не трогаются;
// - размеры комнат по правилам (5×5/6×6/7×7), число комнат по конфигу уровня
//   (content/campaign.js), не константа.
// TODO (Фаза 3): исключение из необратимости для артефакта «Нить Ариадны»
// (core/artifacts-permanent.js) — разовое переоткрытие collapsed-комнаты.
