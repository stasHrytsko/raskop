// ───────────────────────────────────────────────
//  UI артефактов — слоты, активация, цена следующего применения
//  Эпик: H, I (tasks.md) · Статус: заглушка — реализация в Фазе 3 (H), Фаза 4 (I)
//  Зависит от: config.js, core/artifacts-permanent.js, core/artifacts-temp.js
//  Заменяет: — (новая сущность)
// ───────────────────────────────────────────────
//
// TODO (Фаза 3): слоты постоянных артефактов в HUD, кнопка активации с ценой
// (activationCost из core/artifacts-permanent.js), выбор одного из 3 после уровня 3.
// TODO (Фаза 4): индикатор временного артефакта уровня (core/artifacts-temp.js).
