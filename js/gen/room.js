// ───────────────────────────────────────────────
//  Генератор одной комнаты — клетки, типы содержимого, числовые подсказки
//  Эпик: C, J (tasks.md) · Статус: заглушка — реализация в Фазе 1 (C), Фаза 4 (J)
//  Зависит от: config.js, gen/rules.js, content/hints.js
//  Заменяет: field.js (newField/clearStart/trapCount — логика переносится и расширяется)
// ───────────────────────────────────────────────
//
// TODO (Фаза 1):
// - генерировать клетку { type: 'empty'|'gold'|'key'|'hint'|'artifact'|'trap', open, clue }
//   вместо текущего { trap, open } (Эпик C);
// - честный первый тап — per-room, не per-dive (перенос clearStart() на вход в комнату);
// - MAX_CLUE 3 → 5.
// TODO (Фаза 4): встраивать тематические подсказки из content/hints.js (Эпик J).
