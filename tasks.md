# Раскоп — техническая декомпозиция concept.txt

Источник: [concept.txt](concept.txt). Цель документа — разложить бизнес-концепт на
инженерные эпики и задачи. Статус реализации по эпикам — раздел 1 ниже и
[raskop/CLAUDE.md](CLAUDE.md).

## 0. Разрыв: что уже есть vs что описывает концепт

Текущая реализация (см. [raskop/CLAUDE.md](CLAUDE.md)) — это **прототип одной комнаты**:
линейный забег из 9 уровней, каждый уровень = одно поле 5×5 (20 безопасных клеток / 5
ловушек), до 3 заходов, плоская квота 60 на всех уровнях, монет и золота как отдельных
сущностей нет («клетка = +1»), артефактов/ключей/комнат нет.

Концепт описывает **кампанию из 100 уровней**, где каждый уровень — гробница из 2–5
комнат с картой, золотом как ресурсом (расходуемым на артефакты и урезающим квоту),
ключами к необязательным комнатам, постоянными и временными артефактами, метавалютой
между уровнями и генератором с защитой от доминирующих стратегий.

**Это не инкремент, а новый слой поверх текущего ядра.** Текущий цикл «тап → цифра →
уйти/рискнуть» — валидная реализация раздела «Формула ядра» и может остаться as-is
внутри одной комнаты; всё остальное (эпики B–N ниже) — новый функционал.

> ✅ **Решено:** берём модель концепта — кампания из **100 уровней**, каждый уровень —
> гробница из **2–5 последовательных комнат**. Единица провала/перегенерации — **уровень
> целиком**, не отдельная комната и не вся кампания:
> - Ловушка в **любой** из комнат уровня (в т.ч. в последней из 4–5) → провал всего
>   уровня: золото уровня обнуляется, все комнаты этого уровня перегенерируются заново,
>   игрок начинает уровень с первой комнаты.
> - Уровни кампании, пройденные **до** текущего, прогресс не теряют — обнуляется только
>   попытка текущего уровня.
> - Модель «забег 9 уровней / провал = обнуление всего забега» (текущий
>   `raskop/CLAUDE.md`) заменяется этой моделью целиком, не сосуществует параллельно.
>
> Это закрывает открытый вопрос ниже (раздел 4) и однозначно фиксирует Эпик O.

> ✅ **Фаза 1 реализована** (Эпики A/B/C/D — гробница из последовательных комнат,
> золото без рюкзака/множителя, честный старт per-room, число ловушек комнаты видно
> игроку). Подробности реализации — статус в разделе 1 и `raskop/CLAUDE.md`.

### Сквозное требование: mobile-first

Игра — **мобильная**, это не опция и не один из таргетов, а рамка для каждого эпика
ниже. Действует правило корневого [`../CLAUDE.md`](../CLAUDE.md) и уже принятое в
[raskop/CLAUDE.md](CLAUDE.md): `max-width:430px`, `touch-action:none` на игровом поле,
тёмная тема через `:root`, без внешних библиотек. Это не только про CSS — новые эпики
добавляют экраны, которых в прототипе ещё не было, и каждый из них должен изначально
проектироваться под маленький тач-экран, а не адаптироваться постфактум:

- **Тач-таргет.** Любая кликабельная клетка/кнопка/слот артефакта — не мельче текущей
  `.c`/`.lv` (комфортный палец на экране ~375–430px). Уменьшать размер плитки, чтобы
  «впихнуть» больше контента в экран, — запрещённый приём; вместо этого — скролл,
  пагинация или сворачивание в компактное представление (см. правки в Эпиках K и L ниже).
- **Нет наведения.** Никаких hover-only состояний/тултипов — на тач-экране их не
  существует. Любая подсказка (цена активации артефакта, содержимое двери) видна сразу
  или по тапу с открытым состоянием, не по hover.
- **Одна активная область прокрутки.** На экране в любой момент — либо скроллится
  поле/карта, либо скроллится страница, никогда оба одновременно (конфликт жестов) —
  уже частично решено `touch-action:none` на `.grid`, новые скроллируемые контейнеры
  (карта кампании, карта гробницы, лист артефактов) обязаны получить то же правило на
  вложенные интерактивные элементы.
- **Основная кнопка — внизу, в зоне большого пальца.** Как сейчас `.btn-leave`/`.btn-go`
  на всю ширину у нижнего края с `env(safe-area-inset-bottom)`. Любой новый первичный
  CTA (активировать артефакт, выбрать дверь, подтвердить необратимый уход из комнаты —
  Эпик A) следует этому же паттерну, а не всплывающей кнопке в углу.
- **Референс вёрстки — 375–430px по ширине.** Экраны новых эпиков (карта кампании на
  100 уровней, карта гробницы, слоты артефактов) в первую очередь макетируются под
  iPhone SE/стандартный Android-экран, а не «сначала desktop-таблица, потом ужать».

Это не отдельный эпик — это критерий приёмки, применимый к UI-части каждого эпика A–O,
в первую очередь к тем, что вводят новые экраны (A, H, I, K, L, M) — под них есть
конкретные технические уточнения на месте, в самих эпиках ниже.

---

## 1. Карта эпиков

| # | Эпик | Статус в коде | Зависит от |
|---|------|----------------|------------|
| A | Модель гробницы: комнаты вместо одного поля | **Фаза 1 готова** (линейно, без веток/размеров/Ариадны) | — |
| B | Золото, квота и расход золота | **Фаза 1 готова** (без рюкзака; `spendGold` без вызовов) | A |
| C | Плиты: типы содержимого + числовые подсказки 1–5 | **Фаза 1 готова** (только gold/trap; key/hint/artifact — будущие фазы) | A |
| D | Видимость информации игроку (HUD «известно/неизвестно») | **Фаза 1 готова** (базовый HUD; артефакты/objective — будущие фазы) | A, B |
| E | Ключи и закрытые комнаты | нет | A |
| F | Видимая близкая награда | нет | A, C |
| G | Постоянное золото и кошелёк между уровнями | нет | B |
| H | Постоянные артефакты (коллекция, слоты, цена активации) | нет | G |
| I | Временные артефакты | нет | A |
| J | Особые тематические подсказки | нет | C |
| K | Комнаты-секторы для больших полей (8×8+) | нет | A |
| L | Прогрессия кампании: 100 уровней, тиры, боссы | нет (есть 9 линейных) | A–J |
| M | Карта обнаруженной гробницы (in-level) | нет | A |
| N | Генератор: справедливость + анти-доминирующие стратегии | частично (только честный старт + MAX_CLUE) | A, C, F |
| O | Победа/поражение: экономика исхода | частично (иная модель — общий забег) | A, G |

---

## Эпик A — Модель гробницы: комнаты вместо одного поля

**Источник:** «Структура кампании», «Комнаты», «Размеры комнат».

Сейчас `S.field` — плоский массив одной сетки. Нужна структура уровня-гробницы:
несколько комнат (2–5 по уровню кампании), переход между ними, необратимое закрытие
пройденной комнаты («проход обваливается»).

> ✅ **Фаза 1 реализовано:** [js/core/tomb.js](js/core/tomb.js) + [js/gen/room.js](js/gen/room.js) —
> `Tomb = {rooms, currentRoomIndex}`, строго линейная последовательность (без `doors`/
> `visited` — веток нет, это Эпик E), `Room.collapsed` при уходе, провал уровня → новый
> `Tomb`. Размер комнаты фиксирован 5×5 (варьирование 5×5/6×6/7×7 — открытый пункт ниже).
> «Нить Ариадны» и UI-подтверждение необратимого ухода — не реализованы (будущие эпики).

**Технические задачи:**
- [ ] Новая модель данных: `Tomb = { rooms: Room[], currentRoomId, visited: Set }`,
      `Room = { size, field, collapsed:boolean, doors: Door[] }`.
- [ ] Единица провала — **уровень целиком** (решение зафиксировано в разделе 0): ловушка
      в любой комнате уровня уничтожает весь `Tomb` текущей попытки (все комнаты, включая
      уже пройденные и `collapsed`) и запускает генерацию нового `Tomb` для этого же
      номера уровня с чистой первой комнаты. Кампания (`levelsCleared`, Эпик G) при этом
      не трогается — обнуляется только объект текущей попытки уровня.
- [ ] Переход между комнатами: выбор соседней/доступной комнаты через дверь; при выходе
      из комнаты — пометить `collapsed=true`, убрать из доступных.
- [ ] Раздельные размеры комнат по правилам: 5×5 (обучение/быстрые), 6×6 (стандарт),
      7×7 (сложные/сокровищницы) — параметризовать генератор поля по размеру комнаты
      (сейчас `SIZE` — глобальная константа, должна стать параметром на комнату).
- [ ] Число комнат на уровень по правилам прогрессии (2 для ранних, 3 стандарт, 4
      сложный, 5 — экспедиция/босс) — конфиг per-level, не константа.
- [ ] Артефакт «Нить Ариадны» (Эпик H) как единственное исключение из необратимости —
      должен уметь «переоткрыть» `collapsed`-комнату один раз.
- [ ] UI: экран комнаты должен явно показывать «это последняя точка возврата» перед
      уходом (подтверждение — необратимое действие).

**Открытые вопросы:** нужна ли отдельная генерация «сложности» на комнату внутри
одного уровня (первая комната легче последней)?

---

## Эпик B — Золото, квота и расход золота

**Источник:** «Цель уровня», «Расход золота».

Сейчас `S.pack`/`S.prog` — рюкзак и прогресс захода внутри одной модели забега; нет
понятия «потратить золото и квота закрывается обратно».

> ✅ **Фаза 1 реализовано:** [js/core/economy.js](js/core/economy.js) — `R.gold` это и есть
> прогресс квоты (рюкзак/множитель убраны из базового цикла, см. решение в разделе 0),
> `spendGold()` есть как единая точка расхода, но пока без вызовов — ждёт Эпик H.

**Технические задачи:**
- [ ] `S.gold` — золото уровня, отдельно от `S.quotaProgress`. Явно: `quotaProgress =
      gold - spentOnThisLevel` (а не просто «накоплено»), т.к. трата уменьшает именно
      прогресс квоты, а не только доступный баланс.
- [ ] Пересчитывать состояние выхода (`exitOpen = quotaProgress >= quota`) при каждой
      трате золота — выход может закрыться повторно после того как был открыт.
- [ ] API расхода золота: единая точка `spendGold(amount, reason)`, вызывается из
      активации артефактов (Эпик H) и покупки услуг в комнатах.
- [ ] HUD должен реагировать на повторное закрытие выхода (анимация/предупреждение —
      см. Эпик D).

---

## Эпик C — Плиты: типы содержимого + числовые подсказки 1–5

**Источник:** «Плиты», «Числовые подсказки».

Сейчас клетка = `{trap, open}`, только 2 состояния. Нужно 6 типов содержимого.

> ✅ **Фаза 1 реализовано:** [js/gen/room.js](js/gen/room.js) + [js/gen/rules.js](js/gen/rules.js) —
> `MAX_CLUE=5`, честный старт (`clearStart`) на первый тап **каждой** комнаты. Генератор
> создаёт только типы `gold`/`trap` — `key`/`hint`/`artifact` осознанно не генерируются,
> пока не реализованы соответствующие эпики (E/H/I/J), чтобы не оставлять нефункциональный
> тапабельный контент.

**Технические задачи:**
- [ ] Модель клетки: `{ type: 'empty'|'gold'|'key'|'hint'|'artifact'|'trap', open,
      clue }` — `clue` считается независимо от `type` (золото/ключ тоже может нести
      число).
- [ ] `MAX_CLUE` поднять с 3 до 5 (см. «Возможные значения: 1,2,3,4,5»); подсказка не
      отображается при 0 (уже так в текущем `trapCount`, перепроверить рендер).
- [ ] Первое открытие в комнате — всегда безопасное (аналог текущего `FIRST_TAP_BLANK`,
      но per-room, не per-dive — перенести `clearStart()` на вход в комнату).
- [ ] Генератор размещения `gold`/`key`/`hint`/`artifact` по клеткам с учётом баланса
      (см. Эпик N: часть золота в низком риске / часть в зоне высокого).
- [ ] Рендер: иконка по типу + число подсказки поверх/рядом (сейчас `render.js` рисует
      только цифру ловушек).

---

## Эпик D — Видимость информации игроку

**Источник:** «Что известно игроку», «Что показывается в интерфейсе».

> ✅ **Фаза 1 реализовано:** [js/ui/room-render.js](js/ui/room-render.js) — HUD показывает
> число ловушек текущей комнаты (разворот прежнего запрета — см. `raskop/CLAUDE.md`),
> квоту и золото. Слоты артефактов, временный артефакт, цена активации, прогресс видимой
> награды — из будущих эпиков, в HUD пока нет.

**Технические задачи:**
- [ ] HUD комнаты: показывать точное число ловушек в комнате (известно заранее, до
      входа и внутри) — новое поле `room.trapCount`, выводится в шапке.
- [ ] Явно НЕ показывать: число сокровищ в комнате, сколько золота ещё скрыто, есть ли
      ключ/артефакт в комнате — проверить, что ни один API/DOM не протекает эту
      информацию (в т.ч. через дебаг-атрибуты).
- [ ] Флажков/ручных меток нет в базовой версии — не реализовывать долгосрочное
      состояние «пометил как подозрительное» (осознанно вне скоупа).
- [ ] HUD-набор по списку концепта: номер уровня, текущая комната, квота, текущее
      золото, число ловушек в комнате, состояние выхода, выбранные постоянные
      артефакты, временный артефакт (если есть), цена следующей активации, прогресс до
      ближайшей видимой награды (см. Эпик F).

---

## Эпик E — Ключи и закрытые комнаты

**Источник:** «Ключи и закрытые комнаты».

**Технические задачи:**
- [ ] Модель двери: `Door = { targetRoomId, locked:boolean }`; на карте гробницы может
      быть несколько запертых дверей, но ключей на уровне — меньше, чем дверей (по
      умолчанию 1 ключ на несколько закрытых комнат → выбор маршрута).
- [ ] Ключ — предмет уровня (не переносится между уровнями), расходуется при открытии
      двери.
- [ ] Генератор: гарантировать, что финальный выход **никогда** не требует ключа
      («не заставлять игрока рисковать ради обязательного ключа к финальному выходу» —
      прямое ограничение генератора, тестируемое).
- [ ] UI выбора двери: показать, что дверь заперта и сколько ключей у игрока, без
      раскрытия содержимого комнаты за дверью.

---

## Эпик F — Видимая близкая награда

**Источник:** «Видимая награда за продолжение».

**Технические задачи:**
- [ ] Модель «объекта прогресса» в комнате, не завязанного на золото напрямую:
      `RoomObjective = { kind, target, current, rewardPreview }` (например «2/3 части
      печати — саркофаг откроется»).
- [ ] Инкремент прогресса объекта при вскрытии определённых клеток (подмножество плит
      комнаты помечено как «относящиеся к объекту» на этапе генерации).
- [ ] UI: конкретная и понятная формулировка награды заранее («открой ещё одну плиту —
      и...»), обновляется в реальном времени в HUD.
- [ ] Это основной рычаг генератора для критерия «после открытия выхода у игрока
      остаётся причина рискнуть» (Эпик N).

---

## Эпик G — Постоянное золото и кошелёк между уровнями

**Источник:** «Постоянное золото».

**Технические задачи:**
- [ ] Метавалюта `wallet` в `localStorage` (аналог текущего `raskop_best`, новый ключ,
      например `raskop_wallet`), не сбрасывается между уровнями/сессиями.
- [ ] При успешном выходе: `wallet += levelGoldRemaining * (firstClear ? 1.0 : 0.2)`.
      Нужен персистентный флаг «уровень уже пройден хотя бы раз» отдельно от `best`
      (сейчас `best` — просто «максимальный достигнутый уровень», не флаг per-level).
- [ ] Модель прогресса кампании: `levelsCleared: Set<number>` вместо (или в дополнение
      к) линейного `best`, т.к. концепт не требует строго линейного забега на уровне
      персистентности золота (только «пройден ли уровень хотя бы раз»).
- [ ] Экран между уровнями/лагерь: трата `wallet` на постоянные артефакты (Эпик H),
      обновление предложений магазина, косметику — минимум API `spendWallet(amount)`.

---

## Эпик H — Постоянные артефакты

**Источник:** «Постоянные артефакты», примеры (Глаз скарабея, Нить Ариадны, Карта
исследователя, Печать хранителя, Идол жадности).

**Технические задачи:**
- [ ] Реестр артефактов: `Artifact = { id, name, slotType, unlockCondition, basePrice,
      activationCost(useCount), effect }`.
- [ ] Выбор первого бесплатного артефакта из 3 после прохождения уровня 3 (триггер по
      `levelsCleared.has(2)`).
- [ ] Слоты: 1 слот изначально, 2-й открывается «в середине кампании» (конкретный
      номер уровня — параметр конфига, не хардкод).
- [ ] Стоимость активации: 1-е использование на уровне бесплатно, далее 1/2/4 золота
      (геометрическая прогрессия по числу применений **в текущем уровне**, сбрасывается
      каждый уровень) — реализовать как чистую функцию `activationCost(useCountThisLevel)`.
- [ ] Каждый артефакт — отдельный модуль эффекта (не общий if/else):
  - [ ] Глаз скарабея: проверка ловушки под клеткой без вскрытия.
  - [ ] Нить Ариадны: разовый возврат в `collapsed`-комнату (см. Эпик A).
  - [ ] Карта исследователя: показать число ловушек в выбранном секторе (требует Эпик K
        либо работает на уровне комнаты).
  - [ ] Печать хранителя: спасает от одной ловушки ценой части золота — перехват в
        `onTrap()` до применения летального исхода.
  - [ ] Идол жадности: возвращает механику множителя ценой доп. ловушки в комнате —
        нужен hook в генератор комнаты при активации (или пере-генерация на лету).
- [ ] Артефакты не расходуются (остаются в коллекции навсегда) — отличать от временных
      (Эпик I).

**Открытые вопросы:** список всех 50 артефактов концепт не даёт (только 5 примеров +
«6-8 простых первых») — контент-дизайн отдельной задачей, не блокирует архитектуру.

---

## Эпик I — Временные артефакты

**Источник:** «Временные артефакты».

**Технические задачи:**
- [ ] Отдельный слот `S.tempArtifact` (максимум 1 одновременно, если не указано иное),
      не пересекается с постоянными слотами.
- [ ] Находка — как ещё один тип содержимого плиты (`type: 'artifact'`, Эпик C).
- [ ] Действует только текущий уровень; исчезает при выходе или смерти (`onTrap` и
      `clearLevel`/выход должны явно чистить `S.tempArtifact`).
- [ ] Примеры к реализации как отдельные эффекты: удвоение следующей монеты, разовый
      показ безопасной плиты, открытие закрытой двери без ключа, сохранение 1 монеты
      после смерти, знание комнаты с ключом.
- [ ] Часть временных артефактов может иметь «проклятие» или стоить золото при
      использовании — переиспользовать `spendGold` из Эпика B.

---

## Эпик J — Особые тематические подсказки

**Источник:** «Особые подсказки».

**Технические задачи:**
- [ ] Текстовые подсказки как отдельный слой над числовыми (не заменяют, дополняют):
      `RoomHint = { text, appliesTo: cellIds[] | rowIndex | region }`.
- [ ] Гарантия истинности на этапе генерации: подсказка генерируется из уже
      зафиксированного расположения ловушек/ключа/золота, а не наоборот (иначе
      «никогда не лгут» нарушается).
- [ ] Появляются только «в более поздних уровнях» — включается по номеру уровня в
      конфиге прогрессии (Эпик L).
- [ ] Флаг для будущего эксперимента (явно вне базового скоупа): автораскрытие большой
      нулевой области + связанный текст — реализовать за feature-флагом, не включать в
      прод без отдельного решения продукта.

---

## Эпик K — Комнаты-секторы для больших полей (8×8+)

**Источник:** «Размеры комнат» (последний абзац).

**Технические задачи:**
- [ ] Для комнат 8×8+ — генератор стен/секторов, делящих поле на подобласти с
      проходами (не сплошная сетка).
- [ ] Модель: `Room.sectors: Sector[]`, `Sector.doors` внутри одной комнаты (мини-граф
      внутри комнаты, отдельно от графа комнат гробницы из Эпика A).
- [ ] Выбор маршрута между секторами как решение игрока — влияет на порядок открытия
      информации (аналог Эпика F, но внутри одной комнаты).
- [ ] **Mobile-first (см. сквозное требование в разделе 0):** вытянутые комнаты
      (8×5, 10×6 — см. арт-референсы `art/`) и секторы 8×8+/10×10 не влезают в
      `max-width:430px` как единая сетка без потери размера клетки. Комната шире
      вьюпорта — горизонтальный скролл **внутри** комнаты (с своим `touch-action`,
      не конфликтующим со скроллом страницы), а не уменьшение клетки ниже текущего
      тач-таргета `.c`.

**Приоритет:** низкий, требуется только для «сложных комнат/сокровищниц» поздней игры
(тиры 71–90+ по прогрессии, Эпик L) — можно отложить до появления остальных систем.

---

## Эпик L — Прогрессия кампании: 100 уровней, тиры, боссы

**Источник:** «Прогрессия 100 уровней».

**Технические задачи:**
- [ ] Конфиг кампании как данные, не код: массив/таблица на 100 уровней с полями
      `{ roomCount, roomSizes[], trapDensity, quota, hiddenGoldRange, featuresEnabled:
      [...] }` — заменяет текущий `quotaFor(n)`/`trapsFor(n)`/`tierFor(n)` (сейчас
      плоские константы на 9 уровней).
- [ ] Тиры и их фичи как флаги конфига (не хардкод в игровой логике):
  - 1–3: обучение, только основной цикл, выбор 1-го артефакта после уровня 3.
  - 4–15: чистое ядро (без ключей/веток).
  - 16–30: ключи, закрытые комнаты, постоянный магазин.
  - 31–50: размеры 5/6/7, временные артефакты, числа 4–5, проклятые комнаты.
  - 51–70: несколько дверей на 1 ключ, артефакты с ценой/недостатками, лимит ходов в
    отдельных комнатах, богатые/безопасные маршруты.
  - 71–90: 2 постоянных слота, большие гробницы, секторные комнаты (Эпик K),
    комбинации правил.
  - 91–100: без новых механик — чистая проверка мастерства.
- [ ] Каждый 10-й уровень — большая гробница/тематический босс: отдельный флаг
      `isBoss` в конфиге уровня, влияет на `roomCount`/размер, не на новую механику.
- [ ] Экран карты кампании (`screen-levels`, сейчас сетка 3×3 на 9 уровней в
      `nav.js: buildMap()`) переработать под 100 уровней: скролл/пагинация по тирам
      (1–3, 4–15, 16–30, ...), текущий уровень — точка входа по умолчанию, пройденные —
      компактно (не полноразмерными плитками, как сейчас). **Mobile-first (раздел 0):**
      компактное представление — это меньше строк/секций в вертикальном скролле, а не
      уменьшение плитки уровня ниже текущего тач-таргета `.lv`.
- [ ] Тайминг как soft-constraint для контент-дизайна/QA, не хард-код: обычный уровень
      ~3–4 мин, первые 1–2 мин, боссы до 5–7 мин — фиксировать как метрику для
      плейтестов (Эпик N), не как таймер в игре.

**Зависимость:** этот эпик — интеграционный, требует готовых A–K как строительных
блоков, которые конфиг включает/выключает по уровню.

---

## Эпик M — Карта обнаруженной гробницы (in-level)

**Источник:** «Структура кампании» («Отдельно можно открыть карту уже обнаруженной
части гробницы»).

**Не путать** с текущей картой забега (`buildMap()` в `js/nav.js`) — та экранная карта
про кампанию из 9 уровней; здесь речь про карту комнат **внутри одного уровня**.

**Технические задачи:**
- [ ] Отдельный UI-оверлей: граф посещённых/текущей/известных-но-не-посещённых комнат
      уровня, без раскрытия содержимого непосещённых.
- [ ] Данные берутся из `Tomb.rooms`/`visited` (Эпик A) — чисто презентационная задача
      поверх готовой модели гробницы.

---

## Эпик N — Генератор: справедливость и анти-доминирующие стратегии

**Источник:** «Правила генератора», «Защита от доминирующих стратегий», «Критерии
успешного ядра».

Текущий генератор (`field.js: newField`) уже покрывает 2 из ~9 правил (честный первый
ход через `clearStart`, ограничение подсказки через `MAX_CLUE`+ретрай). Остальное — не
реализовано.

**Технические задачи (по пунктам концепта):**
- [ ] Не создавать длинные серии очевидных механических нажатий — метрика на этапе
      генерации (напр. доля клеток с однозначным выводом vs требующих реального
      решения) + ретрай, аналогично текущему `safeNumbersOK`.
- [ ] Не создавать огромные пустые области — ограничение на размер связной области
      нулевых клеток при генерации.
- [ ] Распределять золото между безопасными и рискованными зонами — генератор должен
      считать «риск-профиль» клетки (число соседних ловушек) и балансировать
      размещение `gold`/`key`/`artifact` по профилю, не чисто случайно.
- [ ] Гарантировать, что золота физически хватает на квоту после разумных расходов —
      инвариант генератора: `hiddenGold >= quota + expectedSpend`, с явным тестом/ассертом
      на этапе генерации уровня (аналог retry-цикла в `newField`).
- [ ] Создавать хотя бы одну добровольную рискованную возможность — минимум 1 клетка с
      высокой наградой в зоне высокого риска, отмечаемая на этапе генерации.
- [ ] Не заставлять рисковать ради обязательного ключа к финальному выходу — уже
      отмечено в Эпике E, здесь — юнит-тест генератора.
- [ ] Анти-доминирующие рычаги как системные требования (не что-то новое, а свойства
      уже спроектированных эпиков — фиксирую здесь как чек-лист приёмки):
  - [ ] число сокровищ скрыто (Эпик D);
  - [ ] комнаты необратимо закрываются (Эпик A);
  - [ ] активации артефактов дорожают (Эпик H);
  - [ ] трата золота уменьшает квоту (Эпик B);
  - [ ] нельзя купить все предложения на уровне (баланс: `totalOfferCost > expectedSpendableGold`);
  - [ ] доп. золото требует доп. риска (Эпик N, п. риск-профиль выше);
  - [ ] повторные прохождения дают 20% (Эпик G);
  - [ ] сильные артефакты имеют цену/лимит/недостаток (Эпик H, дизайн per-artifact);
  - [ ] число ловушек и типы комнат меняются по кампании (Эпик L).
- [ ] Критерии успешного ядра как измеримые QA-сценарии, а не только текст:
  - [ ] сильный игрок (эвристика на числовых подсказках) статистически проходит уровень
        чаще случайного бота — симуляционный тест по образцу текущего Монте-Карло
        баланса (см. git-историю: коммит про баланс Монте-Карло уже есть для старой
        модели, паттерн переиспользуем);
  - [ ] средняя длительность уровня по тирам укладывается в целевые 1–2/3–4/5–7 мин на
        плейтестах.

---

## Эпик O — Победа/поражение: экономика исхода

**Источник:** «Победа и проигрыш», «Ловушки» (последствия смерти).

**Технические задачи:**
- [ ] Победа: набрал квоту → выход открыт → дошёл до выхода → покинул гробницу →
      `wallet += remainingGold * multiplier` (см. Эпик G) — отделить «квота набрана» от
      «фактически вышел» (сейчас в `game.js` эти события слиты в `clearLevel()`).
- [ ] Поражение (вскрытие ловушки в любой комнате уровня, включая последнюю из 4–5):
      золото уровня обнуляется, временный артефакт исчезает (Эпик I), **весь `Tomb`
      текущей попытки перегенерируется** (все комнаты уровня, не одна), игрок начинает
      уровень заново с первой комнаты. **Прогресс кампании (пройденные уровни 1..N-1) не
      теряется** — решение зафиксировано в разделе 0, заменяет текущую модель забега
      (`raskop/CLAUDE.md`), где ловушка при исчерпании заходов обнуляла весь прогресс до
      уровня 1.
- [ ] Постоянные артефакты и `wallet` переживают смерть уровня всегда (по определению
      «постоянных») — проверить, что ни один код-путь смерти их не трогает.

---

## 2. Иерархия проекта (целевая архитектура)

Целевая структура `js/` под модель «100 уровней, гробница из 2–5 комнат» (эпики A–O).
CSS пока не разбивается отдельно от `css/styles.css` — разбивка по модулям появится
вместе с разметкой конкретных UI-эпиков (Фазы 1–5 ниже), сейчас это были бы классы без
потребителя в HTML.

Проект остаётся на **классических скриптах, не ES-модулях** (см. `raskop/CLAUDE.md`) —
общий global scope, работает по `file://` и на GitHub Pages без сборки. Порядок
`<script>` в `index.html` — см. `raskop/CLAUDE.md`, раздел «Как связаны модули».

**Фаза 1 подключена и рабочая** (отмечено ✅ ниже); старые `field.js`/`game.js`/`ui.js`/
`render.js`/`nav.js` удалены — их заменили модули из этой структуры. Файлы без ✅ —
по-прежнему заглушки будущих фаз, не подключены к `index.html`.

```
js/
├── config.js                    ✅ константы + $()
├── content/                      данные кампании и контента, без логики (будущие фазы)
│   ├── campaign.js               таблица уровней 1–100: комнаты/размеры/тиры/боссы — L
│   ├── artifacts-permanent.js    реестр постоянных артефактов (схема) — H
│   ├── artifacts-temp.js         реестр временных артефактов (схема) — I
│   └── hints.js                  тексты тематических подсказок по тирам — J
├── gen/                           генерация содержимого комнаты
│   ├── room.js                   ✅ генератор одной комнаты: клетки gold/trap + clueAt() — C
│   ├── sectors.js                разбиение больших комнат (8×8+) на секторы — K (будущее)
│   └── rules.js                  ✅ честный старт, ограничение подсказки, ретраи — N (база)
├── core/                          состояние и правила, без DOM
│   ├── tomb.js                   ✅ модель Tomb: линейный список комнат — A
│   ├── economy.js                ✅ золото/квота, spendGold (без вызовов) — B
│   ├── keys.js                   ключи и закрытые двери — E (будущее)
│   ├── objectives.js             видимая близкая награда — F (будущее)
│   ├── wallet.js                 постоянное золото между уровнями — G (будущее)
│   ├── artifacts-permanent.js    слоты, цена активации, эффекты — H (будущее)
│   ├── artifacts-temp.js         подбор / действие / истечение временных артефактов — I (будущее)
│   └── run.js                    ✅ попытка уровня, победа/поражение — O (база)
├── ui/                            экраны и рендер, без игровой логики
│   ├── nav.js                    ✅ роутинг между 3 страницами
│   ├── campaign-map.js           карта кампании на 100 уровней — L (будущее)
│   ├── tomb-map.js               карта обнаруженных комнат уровня — M (будущее)
│   ├── room-render.js            ✅ сетка комнаты + HUD + кнопки действий
│   ├── artifact-ui.js            слоты и активация артефактов — H, I (будущее)
│   └── popups.js                 ✅ победа/поражение уровня, кампания пройдена
└── app.js                        ✅ bootstrap: loadProgress(); initNav();
```

Соответствие «эпик → файлы» (Эпик D сюда не входит отдельным модулем — это требование к
`ui/room-render.js` + `core/tomb.js`, что именно уходит в HUD, а что скрыто):

| Эпик | Файлы |
|---|---|
| A | `core/tomb.js`, `ui/tomb-map.js` |
| B | `core/economy.js` |
| C | `gen/room.js` |
| D | `ui/room-render.js`, `core/tomb.js` (без отдельного файла) |
| E | `core/keys.js` |
| F | `core/objectives.js` |
| G | `core/wallet.js` |
| H | `content/artifacts-permanent.js`, `core/artifacts-permanent.js`, `ui/artifact-ui.js` |
| I | `content/artifacts-temp.js`, `core/artifacts-temp.js` |
| J | `content/hints.js`, `gen/room.js` |
| K | `gen/sectors.js` |
| L | `content/campaign.js`, `ui/campaign-map.js` |
| M | `ui/tomb-map.js` |
| N | `gen/rules.js` |
| O | `core/run.js` |

**Порядок подключения при миграции** (зависимости слоёв, снизу вверх): `config →
content/* → gen/rules → gen/room → gen/sectors → core/tomb → core/economy → core/keys →
core/objectives → core/wallet → core/artifacts-* → core/run → ui/popups →
ui/room-render → ui/artifact-ui → ui/tomb-map → ui/campaign-map → ui/nav → app`.

Файлы без ✅ существуют на диске как заглушки (заголовок-комментарий: назначение, эпик,
зависимости) — реализация приходит по фазам из следующего раздела.

---

## 3. Предлагаемая фазировка

Эпики технически связаны через A (модель гробницы) и B (золото/квота) — почти всё
остальное на них опирается. Дальше можно параллелить.

| Фаза | Эпики | Что получаем |
|------|-------|---------------|
| 1 ✅ | A, B, C, D | **Готово.** Гробница из нескольких комнат с золотом-ресурсом — ядро концепта на месте текущего «одна комната» |
| 2 | E, F, O | Маршрутизация через ключи + видимая близкая награда + честная экономика исхода |
| 3 | G, H | Метавалюта и постоянные артефакты (после этого уже играбельная вертикальная петля «уровень → апгрейд → уровень») |
| 4 | I, J, M | Временные артефакты, тематические подсказки, внутриуровневая карта — глубина внутри уровня |
| 5 | L | Конфиг кампании на 100 уровней, тиры, боссы — сборка всего в прогрессию |
| 6 | K, N (доп. правила генератора) | Секторные комнаты, полная анти-доминирующая защита, симуляционные QA-тесты |

Фазы 1–3 достаточно, чтобы получить играбельный вертикальный срез, отличимый от
текущего прототипа и проверяющий «Критерии успешного ядра» на малом наборе уровней,
до вложения в контент на 100 уровней (Фаза 5).

## 4. Что решить до старта разработки

~~1. Модель провала уровня~~ — **решено** (раздел 0): провал = перегенерация всего
   уровня (2–5 комнат), прогресс кампании не теряется; модель «забег 9 уровней»
   заменяется полностью, не сосуществует параллельно.

1. Список артефактов за пределами 5 примеров из concept.txt — контент-задача, не
   блокирует Эпики A–G, но блокирует Фазу 3 целиком (нужен реестр минимум 6–8 «простых»
   артефактов для первых уровней, см. Эпик H).
2. Экран «карта кампании» (сейчас `screen-levels` — сетка 3×3 на 9 уровней, `nav.js:
   buildMap()`) нужно спроектировать заново под 100 уровней — сетка не масштабируется
   визуально. Не заходит ни в один эпик A–O явно; довесить в Эпик L как отдельную задачу
   при переходе к Фазе 5.
